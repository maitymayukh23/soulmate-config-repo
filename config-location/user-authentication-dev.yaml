server:
  port: 64100
spring:
  datasource:
    url: jdbc.mysql://localhost:3306/soulmateDB
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Root@5222
  jpa:
    hibernate:
      ddl-auto: update
      show-sql: true
      properties:
        hibernate:
          dialect: org.hibernate.dialect.MySQL8Dialect
